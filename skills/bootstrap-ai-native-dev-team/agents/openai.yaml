interface:
  display_name: "AI-Native Dev Team Bootstrap"
  short_description: "Route lean AI teams with cost-aware governance"
  default_prompt: "Use $bootstrap-ai-native-dev-team to route this work to the smallest cost-effective delivery topology and propose only the governance it needs."
policy:
  allow_implicit_invocation: true
