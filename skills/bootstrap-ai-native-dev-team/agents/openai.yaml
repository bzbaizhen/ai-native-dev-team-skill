interface:
  display_name: "AI-Native Dev Team Bootstrap"
  short_description: "Route Core, Controlled, and explicit audit work"
  default_prompt: "Use $bootstrap-ai-native-dev-team to select the smallest Core or Controlled topology, or an explicit Release Audit path, and propose only the governance it needs."
policy:
  allow_implicit_invocation: true
