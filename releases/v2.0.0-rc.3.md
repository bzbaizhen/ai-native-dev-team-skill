# v2.0.0-rc.3 candidate

Status: local release candidate. This record does not indicate a Push, Tag, GitHub Release, installation update, stable V2 acceptance, or a counted real trial.

## Baseline

- RC.2 anchor-integrity baseline: `7aca66fa105f70350a3771577c6f13a1109881c1`.
- Immutable V1 recovery point: Tag `v1.0.0` at Commit `1ebec8135937fd63d03daf3cdff4e0b7cb740490`.

## Candidate addition

RC.3 closes a V1-comparability fail-open:

- the anchor freeze now registers each usable V1 baseline identity, exact C/R/topology stratum, evidence path, evidence SHA-256, stable-release eligibility, and formal-efficiency eligibility;
- the verifier reads and hashes every baseline evidence object from the exact freeze Commit;
- comparable receipts, project `task_ready` events, and Manifest trials must name the same frozen baseline identity and stratum;
- missing, unknown, disabled, digest-mismatched, or stratum-inconsistent baselines fail closed;
- a case-level baseline may support the five-task stable release gate while remaining explicitly ineligible for a formal efficiency claim;
- the 15-task formal sample counter now includes only trials whose frozen V1 baseline has reconstructable efficiency denominators.

## Dogfood boundary

This release-gate correction changes the Skill candidate and therefore cannot count toward its own five-task sample. Formal trials start only after the final RC.3 Commit and external freeze/head control are fixed.

## Deliberately not claimed

- Stable `v2.0.0` acceptance or measured efficiency improvement.
- That the existing aggregate V1 cases already provide task-level denominators for every C/R/topology stratum.
- Any real-task count toward the five-task gate.
- Push, Tag, GitHub Release, installation update, or public publication.
